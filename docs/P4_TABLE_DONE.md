# P4 implemented surface

Implementation branch contains the engineering snapshot controller, single-cursor Table workflow, dense/virtualized table UI, scope and sort controls, summary strip, Primary/Secondary consistency, and alpha.11 build/release workflow updates. Merge remains gated on cross-platform CI, CodeQL and Windows packaging.
