# P4 scope lock

In scope: single Table Cursor, dense engineering snapshot columns, all-electrical default scope, scope filters, sorting, summary strip, cached one-cycle metrics, virtualized rows, Primary/Secondary consistency, alpha.11 packaging.

Out of scope: sequence components, power/frequency rows, CSV export, user-defined formulas, multi-record comparison. These remain later workstation gates.
